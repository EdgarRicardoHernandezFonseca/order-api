spring:
  application:
    name: order-api

  profiles:
    active: local  # default (puedes cambiarlo)

  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

logging:
  level:
    org.springframework: INFO
